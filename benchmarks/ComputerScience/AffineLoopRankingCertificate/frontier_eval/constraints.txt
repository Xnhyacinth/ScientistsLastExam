1) Only edit solution.py. Keep build_ranking(instance).
2) Return a lexicographic ranking tuple: {"components": [{"r","s","delta"}, ...], "decrease_index", "nonneg_lambdas", "decrease_lambdas"}. Integers or [numerator, denominator] pairs. Floats are rejected, not rounded. Each r must have exact 1-norm 1. Each delta must be positive. decrease_index has one integer per transition. nonneg_lambdas and decrease_lambdas are [transition][component][guard].
3) Entries must stay inside instance["max_numerator"] and instance["max_denominator"]. At most instance["max_components"] ranking components. instance["transitions"] is a nested-reset affine system: each entry has name, guards ({g,d} half-spaces), A and b. instance["dimension"] is 6, 8, 9 or 12. instance["name"] is a label.
4) NumPy/SciPy only. Deterministic CPU code. No network/process creation. Do not read verification/ or frontier_eval/.

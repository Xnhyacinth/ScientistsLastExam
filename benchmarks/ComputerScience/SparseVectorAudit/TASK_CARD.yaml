schema_version: 2
scientific_question: Decide whether a deployed implementation of the sparse vector technique, AboveThreshold
  with a cutoff, keeps the (epsilon, delta)-differential privacy its curator claims for it, by running
  it on query vectors of one's choice under a budget of charged runs, and either name two neighbouring
  query vectors and a set of outputs whose exact probabilities break the claim or decline.
artifact: A verdict, and for a violation two neighbouring query vectors and a list of output strings,
  with a confidence and an optional refusal.
oracle:
  model: the specification is AboveThreshold with a cutoff c over 8 to 12 counting queries in 0..100,
    threshold 50, threshold noise DLap(2/epsilon) drawn once, query noise DLap(4c/epsilon), epsilon 1
    and delta 0.001; each world deploys an implementation that is the specification, or scales one noise
    wrongly (query noise at one half, one third or 0.6 of its scale, threshold noise at one half), or
    skips the query noise for queries beyond an unpublished offset from the threshold at an unpublished
    set of positions; the compliant-branch deployments skip it only where a correct comparison dissents
    with probability small enough that the coupling bound on delta stays below 0.001; the oracle computes
    deterministic output probabilities by finite discrete-threshold enumeration truncated at +/-1200 and
    scores numerical privacy loss; independently checked geometric-tail bounds are documented in references/compliance_argument.md,
    and this is not a formal real-arithmetic certificate
  feasibility: charged runs of the deployed implementation under a budget of 200000 per world, each run's
    randomness seeded by the world, the vector and how many times that vector has been run, overspend
    failing closed; a witness is two neighbouring vectors and a non-empty set of distinct outputs; its
    loss uses that finite enumeration, the larger over both orders of ln((P1 - delta) / P2); calibrated
    refusal on the worlds where the claim holds
  deterministic: true
normalization:
  baseline: the classic pair, the first query one above the threshold and every other one below against
    every query at the threshold, run 2000 times each, the single output with the largest smoothed count
    ratio claimed as a violation, never declining
  reference: verification/reference_positional.py packages the contributor's existing all-position scan
    with its original public helpers, StatDP-style screened library and fresh Clopper-Pearson confirmation,
    without parameter changes. In the fixed current Linux sandbox pilot it scores 0.8193980000000003 development
    / 0.72987 held out, with no false claims. The original first-position method is a lower-capability
    control (0.5702832857142858 / 0.47987), not the admission reference. Remaining headroom is finite
    sampling and event selection; no query position is intentionally omitted.
  score: development mechanism recovery normalized above always-abstain; a witness in a violating world
    scores (min(L, 2 epsilon) - epsilon) / (min(A, 2 epsilon) - epsilon) against the builder's anchor
    A, a false witness scores minus one there and zero in a compliant world, declining scores zero and
    one; witness strength, false discovery, refusal, coverage, calibration, runs used and the held-out
    split stay separate
citations:
- id: doi:10.14778/3055330.3055331
  title: Understanding the sparse vector technique for differential privacy
- id: doi:10.1145/3243734.3243818
  title: Detecting Violations of Differential Privacy
- id: doi:10.1145/3243734.3243863
  title: 'DP-Finder: Finding Differential Privacy Violations by Sampling and Optimization'
- id: doi:10.1109/SP40001.2021.00081
  title: 'DP-Sniper: Black-Box Discovery of Differential Privacy Violations using Classifiers'
- id: doi:10.1561/0400000042
  title: The Algorithmic Foundations of Differential Privacy
- id: doi:10.1145/1536414.1536467
  title: On the complexity of differentially private data release
- id: doi:10.1145/2382196.2382264
  title: On significance of the least significant bits for differential privacy
- id: doi:10.1093/biomet/26.4.404
  title: The Use of Confidence or Fiducial Limits Illustrated in the Case of the Binomial
- id: arxiv:2006.07709
  title: 'Auditing Differentially Private Machine Learning: How Private is Private SGD?'
- id: doi:10.1109/SP40001.2021.00069
  title: 'Adversary Instantiation: Lower Bounds for Differentially Private Machine Learning'
- id: doi:10.1137/09076828X
  title: Universally Utility-maximizing Privacy Mechanisms
- id: doi:10.1109/ALLERTON.2018.8636068
  title: Property Testing For Differential Privacy
invariants:
- implemented as specified the mechanism is epsilon-DP (Lyu, Su and Li 2017, Algorithm 1, with the discrete
  Laplace distribution in place of the continuous one); an exact-objective local search finds at most
  0.904 in the compliant worlds, 0.633 at cutoff two, on events of 0.9 to 2.0 per cent against 0.4 to
  0.8, so plug-in maxima over many pairs and events show a violation in a world where the claim holds;
  the baseline is wrong in every world
- the three compliant-branch deployments differ from the specification only when a correct comparison
  would have dissented at 34 to 42 from the threshold; coupled with the specification they are (epsilon,
  eta (1 + e^epsilon))-DP with that bound at 2.7e-4 to 4.4e-4, below the claimed delta, and the straddling
  event that breaks every branch world has a mass of 2.6e-5 to 1.5e-4 there and a loss of minus infinity;
  a scan at a fixed run count that reads zero dissents as a branch claims them, and its rung scores 0.000
- every violating world has a witness above epsilon, the noise-scale anchors at 1.24 to 1.49 and the branch
  straddles infinite on events of 1.7 to 9.7 per cent; certifying a noise-scale anchor itself with Clopper-Pearson
  bounds takes 4000 to 34000 runs a side at level 0.2 and 8000 to 75000 at 0.05, from a budget of 200000
  shared with the search
- three of the six branch worlds cover the first query and three begin at a later position, 4, 5 or 7;
  the original first-query method declines these three; the existing complete positional method, now packaged
  as the reference, historically scores 0.819 and 0.730 held out with no false discovery on the graded
  seeds
- a false witness costs one world wherever it is made, minus one in a violating world and zero where declining
  scores one; without the first half the reference without its confirmation tied it at 0.570, and with
  it that rung scores 0.427 and 0.000 held out
- the confirmation is load-bearing, since the no-confirmation rung, the probe's best strategy, averages
  0.063 over eight re-drawn seeds with fifty false discoveries in 144 world-runs, where the original first-query
  method averages 0.446 and 0.440 with none
- declining every world scores exactly zero by construction, and so does claiming a violation everywhere;
  confidence feeds only the calibration axis
- runs are charged and overspend fails closed; the candidate receives a closure and not the ledger, so
  it cannot raise its own budget; the same vector run twice gives fresh runs and counts do not depend
  on the order of calls; malformed submissions score zero and never raise out of the evaluator
- held-out split, per-world rows and the discovery axes remain evaluator-only
known_shortcuts: 'Author provenance records a 318-policy in-process search, not independently rerun in
  full. The fixed current Linux pilot at 5c5b358cdf47ec3ce1319fc722cc88bd4afcb0f2 evaluates seven frozen
  programs twice each; all 14 results are valid on all 18 worlds and all seven complete metric pairs repeat
  identically. Full positional reference: 0.8193980000000003 development / 0.72987 held out; first-position-only:
  0.5702832857142858 / 0.47987; no scan: 0.17679285714285717 / 0.22987; no library: 0.5714285714285715
  / 0.5; positional no confirmation: 0.3223138571428571 / 0; original strongest no-library-confirmation
  shortcut: 0.4274261428571428 / 0. The latter two are the executable probes; both are below the unchanged
  10% margin threshold 0.7374582000000003. Their false claims / valid claims are 2/7 and 1/6 on development,
  5/6 and 2/4 held out; the full reference has 0/6 and 0/3. No invalid low score supports this comparison.
  All three fixed independent first proposals are valid and below the complete reference. This bounded
  first-proposal comparison does not measure iterative improvement or general model difficulty.'
review:
  domain: pending_external_differential_privacy
  evaluator_security: The shared stdlib wrapper enforces all-world fail-closed validity and a fresh candidate
    process/tmpfs per world while preserving legal same-world callbacks. At c02e402, all 151 targeted
    Linux tests passed without skips and the separate 13-call contribution gate passed structural/runtime/shortcut
    phases. Independent domain review and certification remain pending.
provenance:
  class: procedural
  target_source: Repository-visible deterministic worlds with twelve development deployments (three compliant,
    two compliant-branch, three noise-scale, four branch) and six sealed held-out deployments (one compliant,
    one compliant-branch, two noise-scale, two branch), each with its own number of queries, cutoff and
    run seed; anchors from an exact-objective search recorded in the evaluator and recomputed on every
    run.
  task_contract_public_before_evaluation: true
  fresh_confirmation_status: first_draws_on_fixed_worlds_complete_no_fresh_server_held_confirmation
novelty_risk:
  level: medium
  rationale: Statistical differential-privacy auditing and sparse-vector failure classes are published.
    The task asks for paid-sample privacy witnesses or calibrated refusal for hidden deployment variants.
    All 47 paper appendix entries and all 78 catalog rows (84 expanded tasks) at Frontier-Engineering
    revision e3fa29c193356af2ce1ec8b3d23ab1a2e2410071 were compared by artifact/oracle; no same problem
    class was found. The full positional reference scores 0.8193980000000003 development, versus the fixed
    strongest historical shortcut 0.4274261428571428; every substantive capability ablation lowers development
    score. Three independent valid first proposals score 0.21659171428571433, 0.2761347142857143 and 0.2652197142857144,
    all below that complete reference. This supports candidate intake under the fixed comparison only;
    external domain review, iterative improvement and fresh server-held confirmation remain pending.
lineage:
  status: complete
  builder_model_ids:
  - claude-opus-5
  builder_scaffolds:
  - claude-code
  calibrator_model_ids:
  - gpt-5.6-sol
  calibration_runs:
  - experiments/sparse_vector_first_draw_review_2026-09-11.json
  calibration_evidence_status: current_or_migration_replayed
  template_source: frontier_eval/run_eval.py is generated by scripts/gen_task.py; the evaluator's per-world
    scoring, split summary and evaluate skeleton follow Phylogenomics/AnomalyZoneSpeciesTree and through
    it StructuralEngineering/ModalDamageAttribution, the repository's exemplar task; the mechanism, the
    exact loss, the deployments, the sampler, the metrics, the reference and the baseline are this task's
    own.
  edits_triggered_by_model: 'Before independent calibration, no endpoint model had seen this task. Six
    edits came from the construction checkpoints. A compliant world that skipped the threshold noise redraw
    had a pure-DP loss of 0.974 and was cut. A halved threshold noise has no compliance proof above cutoff
    one and is used only as a violating world. The first reference confirmed two screened pairs at 0.05
    after 4000 runs a vector and scored 0.400 and 0.250, since the winner''s curse chose events far below
    their plug-in value and 40000 runs a side cannot certify an anchor of 1.24; it now screens at 8000
    and confirms one pair with the rest of the budget at level 0.2. With a false witness scoring zero
    in a violating world the reference without its confirmation tied the reference, so a false witness
    now costs one world wherever it is made. The headroom''s positional scan missed an offset of nine
    and then raised a false alarm sized for the first position only; its down vector is now sized by the
    last position''s reach. And the baseline''s docstring, which the candidate reads, named the family
    of noise-skipping branches; it no longer does, and Task.md names no deviation family. In the 2026-09-11
    admission repair, the contributor''s positional method was promoted without parameter changes, aggregate
    FDR received its correct claim denominator, all-world validity became fail-closed, and the wrapper
    was replaced with the shared launcher. No new model draw informed these repairs. Three independent
    first proposals were then frozen and evaluated at c02e402f7ee709521c5843f6abd3256b6e57f872: development
    0.21659171428571433, 0.2761347142857143 and 0.2652197142857144 against the complete reference 0.8193980000000003.
    All 18 worlds were valid in each proposal. These draws caused documentation and lineage updates only;
    no Task.md, evaluator, reference, worlds, budgets or scoring changed. Retained-artifact compatibility
    is recorded separately; it is not a fresh model draw.'
  shortcut_discoverer: builder_self_audit_with_a_low_dimensional_strategy_probe
  frozen_before_eval: false
  freeze_timestamp: null
long_horizon:
  status: not_tested
  measurement_health_passed: false
  material_headroom_after_2h: false
  evidence: []
construction_audit:
  status: builder_audited_pending_external
  author_domain: claude-opus-5
  reviewer_domain: pending_external
  expert_hours: null
  red_team_rounds: 6
  oracle_disagreement_status: not_quantified
  independent_recomputation_status: The fixed 14-call Linux pilot is recorded in .research/pr80_method_qualification_2026-09-11.json
    with source/runtime/task/candidate hashes and full-metric hashes. All seven pairs are valid and identical,
    with no model calls. The original 318-policy grid and eight-seed robustness remain author in-process
    provenance, not independent replay. The original first-position omission was repaired by packaging
    its already documented positional method, not by lowering a threshold. Independent geometric-tail/compliance
    controls and true process/tmpfs world-isolation regression were added.
metric_contract:
  mechanism:
    metric: witness_strength
    estimand: normalized_valid_witness_strength
    numerator: summed anchor-normalized exact witness strength on violating worlds
    denominator: violating worlds
    denominator_metric: witness_strength_denominator
    direction: higher
  fdr:
    metric: false_discovery_rate
    estimand: false_discovery_rate
    numerator: false valid non-abstaining violation claims
    denominator: all valid non-abstaining violation claims
    denominator_metric: false_discovery_denominator
    direction: lower
  refusal:
    metric: correct_refusal_rate
    estimand: compliant_world_refusal_rate
    numerator: correct refusals on compliant and compliant-branch worlds
    denominator: compliant and compliant-branch worlds
    denominator_metric: correct_refusal_denominator
    direction: higher
  coverage:
    metric: discovery_coverage
    estimand: violating_world_claim_coverage
    numerator: valid non-abstaining claims on violating worlds
    denominator: violating worlds
    denominator_metric: discovery_denominator
    direction: higher
shortcut_probe:
  schema_version: 1
  metric: combined_score
  reference:
    candidate: verification/reference_positional.py
    expected_score: 0.8193980000000003
  probes:
  - id: legacy_best_without_library_confirmation
    candidate: verification/probe_legacy_no_confirmation.py
    expected_score: 0.4274261428571428
  - id: positional_without_confirmation
    candidate: verification/probe_positional_no_confirmation.py
    expected_score: 0.3223138571428571
  relative_margin: 0.1
  score_tolerance: 1.0e-06
evaluation_budget:
  timeout_seconds: 300
  sample_runs_per_world: 200000
  scope: each callback charges its requested run count; overspend is sticky and invalidates; wrapper timeout
    is explicit
